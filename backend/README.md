config file = database configuration

model = define models and schema of databse

routes = define all the apis

contollers = handles request and response from the frontend

services = crud operations in databse